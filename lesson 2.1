#include <iostream>
#include <fstream>

using namespace std;

int main()
{
fstream file("m.txt",ios::out|ios::binary);
int arr[5]={1,2,3,4,5};

    if (!file) {
        cerr << "Dosya açılamadı!" << endl;
        return 1;
    }
for(int i=0;i<5;i++){

    file.write((char *)&arr[i],sizeof(x değişkenini neden attın
                                    int));
}


    return 0;
}
